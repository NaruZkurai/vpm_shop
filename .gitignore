# Build artifacts
/target

# Runtime logs
/logs

# Config file with real URLs/credentials (use vpm-shop.conf.example as a template)
/vpm-shop.conf

# Category-repo config — holds personal domains (use repos.conf.example as a template)
/repos.conf

# Personal to-upload notes / shop data — never commit
/.checklist.json

# Local default shop dir (./mnt/shop) — package data, not source
/mnt

# Compiled VCC validator (.NET) — build/deploy artifact, not source
/validator

/.vscode