# Build artifacts
/target

# Runtime logs
/logs

# Config file with real URLs/credentials (use vpm-shop.conf.example as a template)
/vpm-shop.conf

# Personal to-upload notes / shop data — never commit
/.checklist.json

# Local default shop dir (./mnt/shop) — package data, not source
/mnt
